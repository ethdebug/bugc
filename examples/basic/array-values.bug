name ArrayValues;

storage {
  [0] nums: array<uint256>;
  [1] first: uint256;
}

create {
  // Use array literal assignment instead of index writes
  nums = [100, 200, 300];
  first = nums[0];
  /*@test first-element-check
  variables:
    first:
      pointer: { location: storage, slot: 1 }
      value: 100
  */
  /*@test array-values-check
  variables:
    nums:
      values:
        array-length: 3
        element: [100, 200, 300]
  */
  return;
}

code {
}
